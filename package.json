{
  "name": "tsaga",
  "version": "0.1.2-effects.beta1",
  "description": "Typesafe and lightweight way to write functions with asynchronous side-effects that are easily testable.",
  "main": "compiled/lib/index.js",
  "repository": "https://github.com/tp/tsaga.git",
  "author": "Timm Preetz <timm@preetz.us>",
  "license": "(MIT OR Apache-2.0)",
  "devDependencies": {
    "@types/jest": "^23.1.0",
    "@types/lodash": "^4.14.109",
    "@types/nock": "^9.1.3",
    "@types/node-fetch": "^2.1.1",
    "jest": "23",
    "lodash": "^4.17.10",
    "nock": "^9.3.3",
    "node-fetch": "^2.1.2",
    "redux": "^4.0.0",
    "reselect": "^3.0.1",
    "rimraf": "^2.6.3",
    "ts-jest": "^23",
    "tslint": "^5.10.0",
    "typescript": "^3"
  },
  "peerDependencies": {
    "redux": "4.x"
  },
  "scripts": {
    "watch:tsc": "rimraf compiled/ && tsc --watch",
    "watch:jest": "jest --watchAll",
    "lint": "tslint -c tslint.json --project .",
    "ts-check": "tsc -w --noEmit",
    "test": "tsc --noEmit && yarn lint && jest src/test-app",
    "test:updateSnapshots": "jest --updateSnapshot"
  },
  "dependencies": {
    "typescript-fsa": "^3.0.0-beta-2"
  },
  "files": [
    "compiled/lib/**/*"
  ]
}
